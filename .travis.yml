os:
  - linux

language: php

php:
  - '7.4'

before_install:
  - composer self-update
  - composer validate
  - pecl install -f apcu

install:
  - composer install --prefer-dist

script:
  - vendor/bin/phpunit
